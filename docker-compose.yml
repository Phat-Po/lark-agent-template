services:
  agent:
    build: .
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./src:/app/src
      - ./.env:/app/.env
    stdin_open: true
    tty: true
    restart: "no"
